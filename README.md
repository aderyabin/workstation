# Workstation

Install Brew and run bundle:
```bash
./brew.sh
```

Turn OSX:

```bash
 ./osx.sh
```


Install App Store applications:

```bash
./mas.sh
```

Sync files with iCloud:

```bash
./sync.sh
```
